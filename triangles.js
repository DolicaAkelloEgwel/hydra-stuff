shape(3, 0.5, 0)
	.rotate(Math.PI, 0.5)
	.add(o0, 0.75)
	.repeat(() => (Math.sin(time * 0.25) * 2.5) + 3.5, () => (Math.cos(time * 0.25) * 2.5) + 3.5)
	.out(o1)
osc(20, 0.25, 1.5)
	.diff(voronoi(20, 0.5).modulate(o0))
	.mult(o1)
	.out()
